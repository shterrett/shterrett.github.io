+ Build the project with `stack build`. This is required for changes to `site.hs`
+ Build the actual site with markdown, html, and css changes with `stack exec site build`
+ View the site by opening `_site/index.html`
